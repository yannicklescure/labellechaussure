<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>
<% @products.each do |product| %>
  <% if policy(product).show? %>
    <p><%= product.user.email %> (id: <%= product.user.id %>)</p>
  <% end %>
<% end %>

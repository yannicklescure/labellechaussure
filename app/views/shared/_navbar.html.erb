<div class="navbar navbar-expand-sm navbar-light navbar-lewagon d-flex align-items-center p-3">

  <%= link_to raw('<i class="fas fa-shoe-prints"></i> La Belle Chaussure'), root_path, class: "navbar-brand p-0"  %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <div class="d-flex align-items-center d-flex justify-content-between p-3 p-md-0">
      <div class="nav-item mr-3"><i class="far fa-heart"></i></div>
      <div class="nav-item mr-3"><i class="far fa-envelope"></i></div>
      <div class="nav-item mr-3"><i class="far fa-bell"></i></div>
      <div class="nav-item">
        <% unless user_signed_in? %>
          <%= link_to "Login", new_user_session_path, class: 'btn btn-secondary btn-sm' %>
        <% else %>
          <%= link_to "Logout", destroy_user_session_path, method: :delete, class: 'btn btn-alert btn-sm' %>
        <% end %>
        <%= link_to "Start Selling", new_product_path, class: 'btn btn-outline-secondary btn-sm' %>
      </div>
    </div>
  </div>
</div>
